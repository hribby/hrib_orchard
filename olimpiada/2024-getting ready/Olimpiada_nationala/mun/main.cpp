#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main()
{
    int c;
    cin>>c;
    int n;
    cin>>n;
    vector<string>s(n);
    for(int i = 0;i<n;i++){
        cin>>v[i];
        sort(v[i].begin(),v[i].end());
    }
    return 0;
}
